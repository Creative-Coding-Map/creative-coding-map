import WikipediaSvg from '@/assets/icons/wikipedia.svg?react';

const WikipediaIcon = (props: React.ComponentProps<'svg'>) => {
    return <WikipediaSvg {...props} />;
};

export default WikipediaIcon;

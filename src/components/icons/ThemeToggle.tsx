import ThemeToggleSvg from '@/assets/icons/theme-toggle.svg?react';

const ThemeToggle = (props: React.ComponentProps<'svg'>) => {
    return <ThemeToggleSvg {...props} />;
};

export default ThemeToggle;

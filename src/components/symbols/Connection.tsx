import ConnectionSvg from '@/assets/symbols/connection.svg?react';

const Connection = (props: React.ComponentProps<'svg'>) => {
  return <ConnectionSvg {...props} />;
};

export default Connection; 
